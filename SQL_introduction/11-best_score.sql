-- This SQL command selects the 'score' and 'name' columns from 'second_table' and orders the results by 'score' in descending order
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC
